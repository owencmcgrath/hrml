[package]
name = "markup_parser"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
regex = { version = "1.5", features = ["unicode-perl"] }
wasm-bindgen = "0.2"
js-sys = "0.3"
